import { MostrarMilesPipe } from './mostrar-miles.pipe';

describe('MostrarMilesPipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarMilesPipe();
    expect(pipe).toBeTruthy();
  });
});
