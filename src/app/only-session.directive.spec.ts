import { OnlySessionDirective } from './only-session.directive';

describe('OnlySessionDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlySessionDirective();
    expect(directive).toBeTruthy();
  });
});
