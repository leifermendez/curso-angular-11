import { SinImageDirective } from './sin-image.directive';

describe('SinImageDirective', () => {
  it('should create an instance', () => {
    const directive = new SinImageDirective();
    expect(directive).toBeTruthy();
  });
});
