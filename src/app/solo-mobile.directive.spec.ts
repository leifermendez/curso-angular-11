import { SoloMobileDirective } from './solo-mobile.directive';

describe('SoloMobileDirective', () => {
  it('should create an instance', () => {
    const directive = new SoloMobileDirective();
    expect(directive).toBeTruthy();
  });
});
